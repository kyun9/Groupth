<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UsersMapper">
	<insert id = "insertUser" parameterType="vo.UsersVO">
		<selectKey resultType="_int" keyProperty="id" order="BEFORE">
			select users_seq.nextval from dual
		</selectKey>
		insert into users(id, users_id, users_name, password, email, join_date)
		values(#{id},#{users_id},#{users_name},#{password},#{email}, sysdate)
	</insert>

</mapper>