<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GroupMapper">
	<insert id="createGroup" parameterType="vo.GroupVO">
		<selectKey resultType="_int" keyProperty="gid" order="BEFORE">
			select GROUPS_SEQ.nextval from dual
		</selectKey>
		insert into groups(gid, group_name, fid, content, leader, limit_num, img)
		values(#{gid},#{group_name},#{fid},#{leader},#{fid},#{limit_num},#{img})
	</insert>
</mapper>